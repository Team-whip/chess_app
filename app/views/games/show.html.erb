<script>

  $(document).ready(function() {

    // Selecting a Piece
    $('#table_id td').click(function(){
      $this = $(this);
      var pieceSelected = $('#table_id td').hasClass('selected');

          alert("This piece was selected and will be moved.");
    
    });

    // Moving a Piece
    function sendMove ($pieceSelected){
      //Move the piece
    }

  }); 

</script>

<br />
<div class="booyah-box col-xs-10 col-xs-offset-1">
  <h1 class="text-center"><%= @game.name %></h1>

  <table id= "table_id", class = "chess-board">
    <% @board.board.each_with_index do |row, y| %>
      <tr>
	<% row.each_with_index do |column, x| %>

	  <td data-x = <%= x %>, data-y = <%= y %>, class = "text-center chess-square">
	    <%= @board.piece_glyphs(y, x) %>
	  </td>

	<% end %>
      </tr>
    <% end %>
  </table>

</div>
